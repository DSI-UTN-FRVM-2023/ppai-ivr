/** Representa las validaciones que el operador visualiza en la forma de radio buttons para tomar la respuesta del cliente. */
export type ListaValidacion = {
  nombreValidacion: string;
  opciones: string[];
};
